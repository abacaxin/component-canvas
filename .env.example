# SANGRE — cloud mode (B0). Copy to `.env` and fill in from your Supabase project
# (Project Settings → API). Leave unset to run the editor in local-only mode.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
